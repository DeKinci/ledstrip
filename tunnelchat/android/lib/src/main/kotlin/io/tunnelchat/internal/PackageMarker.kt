@file:Suppress("unused")
package io.tunnelchat.internal

// Placeholder to anchor the internal package tree. Subdirectories (ble/, wire/,
// protocol/, blob/, proto/, archive/, stats/, log/) are populated in later phases.
