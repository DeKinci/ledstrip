{
  "name": "ledstrip",
  "private": true,
  "workspaces": [
    "module/*",
    "infra/build",
    "infra/test-runner",
    "script/*",
    "service/gateway/web"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:web": "npx tsx infra/build/src/bundle-web.ts",
    "test": "vitest run --dir module",
    "test:watch": "vitest --dir module",
    "test:device": "node --import tsx infra/test-runner/src/cli.ts",
    "dev:gateway": "npm run dev -w @microproto/gateway-ui",
    "mpcli": "node --import tsx script/microproto-cli/src/main.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
