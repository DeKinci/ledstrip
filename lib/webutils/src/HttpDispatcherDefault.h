#ifndef HTTP_DISPATCHER_DEFAULT_H
#define HTTP_DISPATCHER_DEFAULT_H

#include "HttpDispatcher.h"

// Default global dispatcher instance
// Use this for simple applications that need a single dispatcher
extern HttpDispatcher httpDispatcher;

#endif